spring:
  profiles:
    default: local
  jpa:
    properties:
      hibernate:
        format_sql: true
        show_sql: true
        jdbc:
          time_zone: Asia/Seoul
  jackson:
    time-zone: Asia/Seoul
server:
  timezone: Asia/Seoul

logging:
  level:
    org.springframework.data.elasticsearch: DEBUG
    org.lostnomore.backend.item.elastic: DEBUG
    tracer: TRACE